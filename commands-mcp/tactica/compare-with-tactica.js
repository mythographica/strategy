/**
 * MCP Tool Metadata:
 * {
 *   "name": "compare_with_tactica",
 *   "description": "Compare runtime Mnemonica types with Tactica-generated static types",
 *   "inputSchema": {
 *     "type": "object",
 *     "properties": {
 *       "projectPath": {
 *         "type": "string",
 *         "description": "Absolute path to the project with .tactica/types.ts"
 *       }
 *     },
 *     "required": ["projectPath"]
 *   }
 * }
 */

// This tool is handled server-side, not evaluated in the runtime
// The actual implementation is in the StrategyServer class

null
